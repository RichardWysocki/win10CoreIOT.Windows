using NUnit.Framework;

namespace Management.Tests.Controllers
{
    [TestFixture()]
    public class WhenValidatingCustomerController
    {
        //[Test]
        //public void Index()
        //{
        //    // Arrange
        //    var context = A.Fake<IServiceLayer>();
        //    CustomerController controller = new CustomerController(context);

        //    A.CallTo(() => context.GetData<Customer>(A<string>.Ignored)).Returns(new List<Customer>
        //    {
        //        new Customer {CustomerId = 1, FirstName = "A", LastName = "B"},
        //        new Customer {CustomerId = 2, FirstName = "A", LastName = "B"}
        //    });


        //    // Act
        //      ViewResult result = controller.Index() as ViewResult;

        //    // Assert
        //    Assert.IsNotNull(result);
        //    Assert.IsNotNull(result.Model);
        //    var responseModelCount = (List<Customer>) result.Model;
        //    Assert.That(responseModelCount.Count == 2, $"Model Response was {responseModelCount.Count}.");
        //}


    }
}
